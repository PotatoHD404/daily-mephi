export default function getTutorSvg({
                                        tutor_name,
                                        mephist_rating,
                                        daily_rating,
                                        reviews,
                                        reviews_count,
                                        materials,
                                        materials_count,
                                        rating,
                                        rating_value,
                                        image
                                    }: { tutor_name: string, daily_rating: string, mephist_rating: string, reviews: string, reviews_count: string, materials: string, materials_count: string, rating: string, rating_value: string, image: string }): string {
    return `



  `;
}
